# Maintainer: Alexander F. Rødseth 
# Contributor: Andreas Radke 
# Contributor: rabyte 
# Contributor: Johannes Krampf 
# Contributor: Bartłomiej Piotrowski 

pkgname=midori-rune
_pkgname=midori
pkgver=8.0
pkgrel=1
pkgdesc='Lightweight web browser'
arch=('x86_64' 'armv6h' 'armv7h')
url='https://midori-browser.org/'
license=('LGPL')
depends=('aria2' 'gcr' 'gobject-introspection-runtime' 'libpeas' 'libxss' 
'webkit2gtk')
makedepends=('bzr' 'cmake' 'git' 'gobject-introspection' 'intltool' 'ninja' 'vala' 'glib2' 'json-glib')
optdepends=('gst-plugins-base: HTML5 OGG videos support'
            'gst-plugins-good: HTML5 H264 and WebM videos support'
            'gst-libav: HTML5 H264 videos support')
options=('!emptydirs')
#source=("$_pkgname::git+https://github.com/midori-browser/core.git#\
#	tag=v${pkgver%.0}")
source=("https://github.com/midori-browser/core/releases/download/\
v"$pkgver"/midori-v"$pkgver".tar.gz")
#https://github.com/midori-browser/core/releases/download/v8.0/midori-v8.0.tar.gz
md5sums=('SKIP')

build() {
  mkdir -p build
  cd build
  cmake "../$_pkgname-v$pkgver" \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DCMAKE_INSTALL_LIBDIR=lib \
    -DCMAKE_C_FLAGS="$CFLAGS -fPIC -w" \
    -DVALA_CFLAGS="$CFLAGS -fPIC -w" \
    -DUSE_ZEITGEIST=0 \
    -G Ninja
  ninja
}

package() {
  DESTDIR="$pkgdir" ninja -C build install
}

# getver: launchpad.net/midori/+download
# vim: ts=2 sw=2 et:
